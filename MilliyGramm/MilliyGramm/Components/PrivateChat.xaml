<UserControl x:Class="MilliyGramm.Components.PrivateChat"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MilliyGramm.Components" 
             Height="60" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Ellipse Margin="5">
            <Ellipse.Fill>
                <ImageBrush ImageSource="../Resources/Images/opa.jpg" Stretch="UniformToFill"/>
            </Ellipse.Fill>
        </Ellipse>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Bek Olimjon" Foreground="Snow" FontWeight="DemiBold" VerticalAlignment="Center" Margin="10"/>
            <TextBlock Grid.Row="1" Text="Qalaysanlar?" Foreground="Gray" FontWeight="DemiBold" VerticalAlignment="Center" Margin="10 0 10 10"/>

        </Grid>
        
    </Grid>
</UserControl>
